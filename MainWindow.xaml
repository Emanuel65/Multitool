<Window x:Class="Multitool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Multitool"
        mc:Ignorable="d"
        Title="Multitool" Height="317" Width="709" WindowStartupLocation="CenterScreen" FontSize="20">
    <Window.Effect>
        <DropShadowEffect/>
    </Window.Effect>
    <StackPanel>

        <!--#region Input -->
        <!--First string to compare -->
        <TextBlock Text="String A" 
                   Margin="5, 0">
        </TextBlock>
        <RichTextBox x:Name="rtxtBx_A" 
                 Margin="5" 
                 Height="50" TextChanged="rtxtBx_A_TextChanged"/>

        <!--Second string to compare -->
        <TextBlock Text="String B" 
                   Margin="5, 0">
        </TextBlock>
        <RichTextBox x:Name="rtxtBx_B" 
                 Margin="5" 
                 Height="50" TextChanged="rtxtBx_B_TextChanged"/>
        <!--#endregion-->

        <!--#region Interactive tools -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Compare strings button -->
            <Button x:Name="btn_CompareStrings"
                    Click="btn_CompareStrings_Click"
                    Grid.Column="0"
                    Grid.Row="0"
                    Margin="5" Padding="3" 
                    Content="Compare Strings">
            </Button>

            <!-- Clear content button -->
            <Button x:Name="btn_Clear"
                    Grid.Column="1"
                    Grid.Row="0"
                    Margin="5" Padding="3" 
                    Content="Clear" Click="btn_Clear_Click">
            </Button>

            <!-- Result -->
            <TextBox x:Name="txtBlk_Result"
                     Grid.Column="0"
                     Grid.Row="1"
                     Grid.ColumnSpan="3"
                     Margin="5" Padding="3" 
                     Text="Result"
                     TextAlignment="Center" 
                     IsInactiveSelectionHighlightEnabled="True" FontWeight="Bold" FontSize="18" IsReadOnly="True" IsUndoEnabled="True"/>

            <!-- Region check content button -->
            <Button x:Name="btn_Region"
                    Grid.Column="2"
                    Grid.Row="0"
                    Margin="5" Padding="3" 
                    Content="Region Check">
            </Button>

        </Grid>
        <!--#endregion-->


    </StackPanel>
</Window>
